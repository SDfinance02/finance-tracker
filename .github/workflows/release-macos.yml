name: Publish Finance Tracker macOS

on:
  workflow_dispatch:

jobs:
  publish-macos:
    permissions:
      contents: write
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm install

      - name: Generate application icons
        run: npm run tauri icon src-tauri/icons/app-icon.png

      - name: Build signed universal macOS updater and publish release
        uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          FINANCE_UPDATER_PUBKEY: ${{ vars.FINANCE_UPDATER_PUBKEY }}
          FINANCE_UPDATER_ENDPOINT: https://github.com/${{ github.repository }}/releases/latest/download/latest.json
          APPLE_SIGNING_IDENTITY: '-'
        with:
          tagName: app-v__VERSION__
          releaseName: 'Finance Tracker v__VERSION__'
          releaseBody: 'Finance Tracker v2.6 adds Decision Lab: Monte Carlo wealth paths, P10/P50/P90 confidence bands, FI probability, liquidity stress, sequence-of-returns shock testing, scenario robustness comparison and a local simulation audit trail. Existing local finance data is preserved.'
          releaseDraft: false
          prerelease: false
          uploadUpdaterJson: true
          uploadUpdaterSignatures: true
          retryAttempts: 3
          args: --config src-tauri/tauri.release.conf.json --target universal-apple-darwin
